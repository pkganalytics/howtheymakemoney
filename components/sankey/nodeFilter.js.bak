import { createSlice } from '@reduxjs/toolkit';

const nodeFilterSlice = createSlice({
	name: 'nodeFilter',
	initialState: 6,
	reducers: {
		setnodeFilter1: (state) => 1,
		setnodeFilter2: (state) => 2,
		setnodeFilter3: (state) => 3,
		setnodeFilter4: (state) => 4,
		setnodeFilter5: (state) => 5,
		setnodeFilter6: (state) => 6

	}
})

export const {setnodeFilter1, setnodeFilter2, setnodeFilter3, setnodeFilter4, setnodeFilter5, setnodeFilter6} = nodeFilter.actions;

export default colourSlice.reducer;
